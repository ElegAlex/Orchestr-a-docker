/**
 * FIREBASE DÉSACTIVÉ - Migration vers REST API en cours
 *
 * Ce fichier est conservé uniquement pour la compatibilité avec les anciens composants
 * qui n'ont pas encore été migrés. Il ne se connecte plus à Firebase.
 *
 * Les imports Firebase restent pour éviter les erreurs de compilation,
 * mais aucune connexion n'est établie.
 */

// Exports stub pour compatibilité - NE SE CONNECTE PAS À FIREBASE
export const auth = null as any;
export const db = null as any;
export const storage = null as any;
export const functions = null as any;

const app = null as any;
export default app;
